var express = require('express')
var router = express.Router()

var permissionController =  require("../controllers/permissionController")

// router.get('/permission',permissionController.getData)

module.exports = router

